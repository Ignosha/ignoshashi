#!/usr/bin/env node
/** Real local-EVM runtime harness for the BNB bonding-curve contracts. */
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";
import { Contract, ContractFactory, JsonRpcProvider, Wallet, NonceManager, getAddress } from "ethers";
const root = resolve(import.meta.dirname, "..");
const artifactPath = resolve(root, "src/contracts/abis/BnbSmartChainTestnetTokenFactory.json");
const tokenArtifactPath = resolve(root, "src/contracts/abis/BnbSmartChainTestnetBondingCurve.json");
const ANVIL_DEFAULT_KEY = "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80";
const ANVIL_PLATFORM_KEY = "0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d";
const rpcUrl = process.env.BNB_TEST_RPC_URL?.trim() || "http://127.0.0.1:8545";
function loadArtifact(path, label) { let artifact; try { artifact = JSON.parse(readFileSync(path, "utf8")); } catch { throw new Error(`${label} artifact missing at ${path}; compile artifacts first`); } assert.ok(Array.isArray(artifact.abi), `${label} ABI missing`); assert.match(artifact.bytecode, /^0x[0-9a-f]+$/i, `${label} bytecode missing`); assert.ok(artifact.bytecode.length > 100, `${label} bytecode is empty`); return artifact; }
async function withTimeout(promise, message, ms = 4000) { let timer; try { return await Promise.race([promise, new Promise((_, reject) => { timer = setTimeout(() => reject(new Error(message)), ms); })]); } finally { clearTimeout(timer); } }
async function expectRevert(action, expected) { try { await withTimeout(action(), `revert check timeout (${expected})`); } catch (error) { const text = String(error?.shortMessage || error?.reason || error?.message || error); assert.ok(text.includes(expected), `expected ${expected}, got ${text}`); return; } assert.fail(`expected transaction to revert with ${expected}`); }
async function main() {
 const factoryArtifact = loadArtifact(artifactPath, "BNB factory"); const tokenArtifact = loadArtifact(tokenArtifactPath, "BNB bonding curve");
 let url; try { url = new URL(rpcUrl); } catch { throw new Error(`BNB_TEST_RPC_URL is not a valid URL: ${rpcUrl}`); } if (url.protocol !== "http:" || !["127.0.0.1", "localhost", "::1"].includes(url.hostname)) throw new Error("refusing non-loopback RPC; this harness is local Anvil-only");
 const provider = new JsonRpcProvider(rpcUrl, undefined, { staticNetwork: false }); let chainId; try { chainId = BigInt(await withTimeout(provider.send("eth_chainId", []), "RPC unavailable: could not query eth_chainId")); } catch (error) { throw new Error(`RPC unavailable at ${rpcUrl}: ${error.message}`); } assert.equal(chainId, 31337n, `expected Anvil chain ID 31337, got ${chainId}`);
 const signer = new NonceManager(new Wallet(process.env.BNB_TEST_PRIVATE_KEY?.trim() || ANVIL_DEFAULT_KEY, provider)); const creator = await signer.getAddress(); const platformSigner = new NonceManager(process.env.BNB_TEST_PLATFORM_PRIVATE_KEY?.trim() ? new Wallet(process.env.BNB_TEST_PLATFORM_PRIVATE_KEY.trim(), provider) : new Wallet(ANVIL_PLATFORM_KEY, provider)); const platform = getAddress(process.env.BNB_TEST_PLATFORM_ADDRESS?.trim() || await platformSigner.getAddress()); assert.equal(platform, await platformSigner.getAddress(), "BNB_TEST_PLATFORM_ADDRESS must match the supplied local platform key"); const outsider = Wallet.createRandom().connect(provider);
 const factory = await new ContractFactory(factoryArtifact.abi, factoryArtifact.bytecode, signer).deploy(platform, 100, 5000); await withTimeout(factory.waitForDeployment(), "factory deployment receipt timeout"); const factoryAddress = await factory.getAddress(); const createTx = await factory.createToken("Anvil Meme", "ANV", 10_000, 1_000_000_000n, 1_000_000_000n); const createReceipt = await withTimeout(createTx.wait(), "createToken receipt timeout"); const created = createReceipt.logs.map(log => { try { return factory.interface.parseLog(log); } catch { return null; } }).find(event => event?.name === "TokenCreated"); assert.ok(created, "TokenCreated event missing"); const tokenAddress = getAddress(created.args.token); const token = new Contract(tokenAddress, loadArtifact(tokenArtifactPath, "BNB bonding curve").abi, signer);
 assert.equal(await factory.tokenCount(), 1n); assert.equal(await token.balanceOf(tokenAddress), 10_000n); assert.equal(await token.creator(), creator); const amount = 100n; const [cost, fee] = await token.getBuyCost(amount); const total = cost + fee; await (await token.buy(amount, total, BigInt(Math.floor(Date.now() / 1000) + 300), { value: total })).wait(); assert.equal(await token.balanceOf(creator), amount); assert.equal(await token.trackedGraduationReserve(), cost); assert.equal(await token.feeCredits(platform), fee / 2n); assert.equal(await token.feeCredits(creator), fee - fee / 2n);
 await expectRevert(() => token.buy(1, total, 0, { value: total }), "DEADLINE"); await expectRevert(() => token.buy(1, 0, BigInt(Math.floor(Date.now() / 1000) + 300), { value: total }), "SLIPPAGE"); const sellAmount = 40n; const [proceeds, sellFee] = await token.getSellProceeds(sellAmount); const net = proceeds - sellFee; await (await token.approve(tokenAddress, sellAmount)).wait(); signer.reset(); await (await token.sell(sellAmount, net, BigInt(Math.floor(Date.now() / 1000) + 300))).wait(); assert.equal(await token.balanceOf(creator), amount - sellAmount); assert.equal(await token.trackedGraduationReserve(), cost - net); assert.equal(await token.feeCredits(platform), fee / 2n + sellFee / 2n); assert.equal(await token.feeCredits(creator), fee - fee / 2n + sellFee - sellFee / 2n); await expectRevert(() => token.sell(1, net + 1n, BigInt(Math.floor(Date.now() / 1000) + 300)), "SLIPPAGE"); await expectRevert(() => token.sell(1, 0, 0), "DEADLINE");
 const outsiderToken = token.connect(outsider); await expectRevert(() => outsiderToken.withdrawFees(), "NO_FEES"); await (await token.connect(platformSigner).withdrawFees()).wait(); assert.equal(await token.feeCredits(platform), 0n); await (await token.withdrawFees()).wait(); assert.equal(await token.feeCredits(creator), 0n); await expectRevert(() => token.requestGraduation(outsider.address), "BNB_GRADUATION_GATED"); await expectRevert(() => token.executeGraduation(), "BNB_GRADUATION_GATED"); console.log(`PASS BNB Anvil runtime: chain ${chainId}, factory ${factoryAddress}, token ${tokenAddress}`); console.log("PASS create, custody, buy/sell, fee credits/withdrawal, deadline/slippage, and graduation gates"); }
main().catch(error => { console.error(`FAIL BNB Anvil runtime: ${error.message}`); process.exitCode = 1; });
